class Solution {
    public double myPow(double x, int n) {
        // Use the Math.pow() method to calculate the power function
        x = Math.pow(x, n);
        // Return the result
        return x;
    }
}
